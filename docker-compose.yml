services:
  slacksmith:
    build: .
    env_file: .env
    volumes:
      - ./agents:/app/agents
      - ./mcp.config.json:/app/mcp.config.json:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "process.exit(0)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
